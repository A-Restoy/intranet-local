name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [raspberry]
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Ejecutar script de despliegue
        run: |
          chmod +x /home/arestoy/scripts/deploy-intranet-site.sh
          /home/arestoy/scripts/deploy-intranet-site.sh
